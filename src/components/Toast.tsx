import type React from "react"

const Toast: React.FC = () => {
  // Toast is now handled within ContactModal component
  // This component exists for consistency with the layout structure
  return null
}

export default Toast
